import 'package:flutter/material.dart';
import 'package:firebase_database/firebase_database.dart';
import 'package:curved_navigation_bar/curved_navigation_bar.dart';
import 'package:cloud_firestore/cloud_firestore.dart';// Import the Firebase Realtime Database package.

class wattendence extends StatefulWidget {
  const wattendence({Key? key}) : super(key: key); // Define a stateful widget for the warden page.

  @override
  _wattendenceState createState() => _wattendenceState(); // Create the state for the widget.
}

class _wattendenceState extends State<wattendence> {
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}