import 'package:flutter/material.dart';
import 'package:firebase_database/firebase_database.dart';
import 'package:curved_navigation_bar/curved_navigation_bar.dart';
import 'package:cloud_firestore/cloud_firestore.dart';// Import the Firebase Realtime Database package.

class wroom extends StatefulWidget {
  const wroom({Key? key}) : super(key: key); // Define a stateful widget for the warden page.

  @override
  _wroomState createState() => _wroomState(); // Create the state for the widget.
}

class _wroomState extends State<wroom> {
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}